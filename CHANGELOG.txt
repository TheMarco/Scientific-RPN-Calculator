- made all memory registers persistent and working properly
- fixed a bug causing swapXY not to work fully correctly
- fixed a bug causing the EEX button to not work correctly with single digit numbers
- fixed a bug where rounding caused very small numbers to be rounded to 0
- fixed a wrong value in the converter for velocity
- made sure conversions get rounded to prevent JS rounding errors
- new feature: toggle fullscreen mode to prevent notifications from messing up your calculations
- UI improvements (screen and background)



- Pixi compatibility
- eliminated all rounding errors
- fixed a bug that broke the statistics functions